{
  "name": "vibe-canvas-extensions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the vibe Agent Canvas extensions.",
  "scripts": {
    "build": "npm --prefix kanban-manager run build",
    "test": "npm --prefix kanban-manager run check",
    "validate": "node validate-extension.mjs kanban-manager"
  }
}
